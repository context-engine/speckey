import type { ParseOptions } from "./types";

/**
 * Parse CLI arguments into ParseOptions.
 * @throws Error if unknown flag or missing value
 */
export function parseArgs(_args: string[]): ParseOptions {
    throw new Error("Not implemented");
}
